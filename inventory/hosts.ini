# Le pilotage du cluster Kubernetes se fait via l'API Kubernetes (module
# kubernetes.core), pas via SSH : un seul hôte "local" suffit donc, exécuté
# depuis le poste où se trouve le kubeconfig (control node Ansible).
[k8s_control]
localhost ansible_connection=local
